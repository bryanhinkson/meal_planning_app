import 'package:flutter/material.dart';

class PrepHome extends StatelessWidget {
  const PrepHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Prep Page');
  }
}
