import 'package:flutter/material.dart';

class PlanHome extends StatelessWidget {
  const PlanHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Plan Page');
  }
}
