import 'package:flutter/material.dart';

class ShoppingHome extends StatelessWidget {
  const ShoppingHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Shopping Page');
  }
}
